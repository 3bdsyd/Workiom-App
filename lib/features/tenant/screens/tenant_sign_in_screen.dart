import 'package:flutter/material.dart';

class TenantSignInScreen extends StatelessWidget {
  const TenantSignInScreen({super.key});

  static const String routePath = '/tenant-sign-in';
  static const String routeName = 'tenantSignIn';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Tenant Sign-In (TODO: implement)')),
    );
  }
}
